name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
# ────────────────────────── LINT + TEST ──────────────────────────
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
    # 1. Получаем код репозитория
    - uses: actions/checkout@v4

    # 2. Ставим конкретную версию Python
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    # 3. Кеш pip-пакетов
    - name: Cache pip
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: ${{ runner.os }}-pip-

    # 4. Устанавливаем зависимости + линтеры
    - name: Install deps
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install flake8 black pytest

    # 5. Flake8
    - name: Run Flake8
      run: flake8 --config .flake8 .

    # 6. Black (только проверка форматирования)
    - name: Check formatting with Black
      run: black --check .

    # 7. Pytest (код 5 → нет тестов  → считаем успехом)
    - name: Run tests (tolerate no-tests)
      run: |
        set +e
        python -m pytest -q
        status=$?
        if [ "$status" = "5" ]; then
          echo "🛈  Pytest: no tests collected → ok"
          exit 0
        fi
        exit $status

# ──────────────────────────── DOCKER ────────────────────────────
  docker-build:
    if: github.event_name == 'push'           # только push (не PR)
    needs: lint-and-test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - uses: actions/checkout@v4

    # логин в Docker Hub
    - name: Docker login
      uses: docker/login-action@v3
      with:
        registry: docker.io
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    # тег для образа
    - name: Extract tag (YYYYMMDD-HHMM)
      id: tag
      run: echo "TAG=$(date +'%Y%m%d-%H%M')" >>"$GITHUB_OUTPUT"

    # сборка и push
    - name: Build & push image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: |
          docker.io/${{ secrets.DOCKERHUB_USERNAME }}/linux-quiz-bot:${{ steps.tag.outputs.TAG }}
          docker.io/${{ secrets.DOCKERHUB_USERNAME }}/linux-quiz-bot:latest
